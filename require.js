// the page allow people to add the a post and username to a web page.
// it allows people to edit its own conetent.
// it allows a person to delete its own content.
// it show the latest update Time.


const d = new Date();

let dateStr = `${d.getHours()}:${d.getMinutes()} ${d.getMonth()+1}/${d.getFullYear()} AEDT`

// console.log(dateStr);

console.log(dateStr);